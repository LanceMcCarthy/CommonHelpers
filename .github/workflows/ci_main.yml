name: Main

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'src/CommonHelpers/**/*'
      - 'src/CommonHelpers.Tests/**/*'
      # - '.github/workflows/ci_main.yml'

env:
  DOTNET_SDK_VERSION: '10.0.x'
  PROJ_PATH: 'src/CommonHelpers/CommonHelpers.csproj'

jobs:
  build:
    runs-on: windows-latest
    name: 'CI - Main'
    outputs:
      app_version: ${{steps.get-version.outputs.package-version}}
    steps:
    - name: Harden Runner
      uses: step-security/harden-runner@v2.21.1 #immutable
      with:
        egress-policy: audit

    - name: Checkout
      uses: actions/checkout@v7

    - name: Install .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: ${{env.DOTNET_SDK_VERSION}}

    # Remove nuget.config now, and only add project bin package sources later after they're created
    - name: Update package sources for CI
      run: rm ${{github.workspace}}/src/nuget.config

    - name: Setup MSBuild.exe
      uses: microsoft/setup-msbuild@v3

    - name: Add Alpha (-alpha) suffix to Version Number
      run : |
        [xml]$projXml = get-content "${{env.PROJ_PATH}}"
        $currentVer = $projXml.Project.PropertyGroup.Version
        $newVer = $currentVer + "-alpha.${{github.run_number}}"
        $projXml.Project.PropertyGroup.Version = $newVer
        $projXml.save("${{env.PROJ_PATH}}")

    - name: Restore & Build CommonHelpers
      working-directory: src/CommonHelpers
      run: |
        dotnet restore --runtime any --ignore-failed-sources
        dotnet build -c Release --no-restore

    - name: Add CommonHelpers Bin to NuGet Package Sources
      run: dotnet nuget add source "${{github.workspace}}/src/CommonHelpers/bin/Release" --name "CommonHelpers_Bin"

    - name: Get Package File Path
      id: get-package-path
      run : |
        $currentDirectory = Get-Location
        $outputFolder = Join-Path -Path $currentDirectory -ChildPath "src/CommonHelpers/bin/Release/"
        $match = Get-ChildItem $outputFolder -Recurse -Force -Include *.nupkg
        echo "nuget-package-path=$match" >> $env:GITHUB_OUTPUT

    - name: Verify Package Path
      run: Write-Output Package path is "${{steps.get-package-path.outputs.nuget-package-path}}"

    - name: Restore & Build CommonHelpers.Tests
      working-directory: src/CommonHelpers.Tests
      run: |
        dotnet restore --runtime any --ignore-failed-sources
        dotnet build -c Release --no-restore

    - name: Run Tests
      working-directory: src/CommonHelpers.Tests
      run: dotnet test --runtime any -c Release --logger GitHubActions --blame-crash --collect:"XPlat Code Coverage" -- DataCollectionRunSettings.DataCollectors.DataCollector.Configuration.Format=opencover

    - name: Get Version From Project
      uses: kzrnm/get-net-sdk-project-versions-action@v3
      id: get-version
      with:
        proj-path: ${{env.PROJ_PATH}}

    - name: Upload CommonHelpers NuGet package artifact
      uses: actions/upload-artifact@v7
      with:
        name: "CommonHelpers.${{steps.get-version.outputs.package-version}}.nupkg"
        path: ${{steps.get-package-path.outputs.nuget-package-path}}

  codesign:
    name: Sign NuGet packages
    needs: [build]
    runs-on: [self-hosted, x64, digicert-enabled]
    env:
      PKG_NAME: "CommonHelpers.${{needs.build.outputs.app_version}}.nupkg"
    steps:
    - name: Checkout
      uses: actions/checkout@v7

    - name: Install .NET
      uses: actions/setup-dotnet@v6
      with:
        dotnet-version: ${{env.DOTNET_SDK_VERSION}}

    - name: Clean bundle workspace
      run: Remove-Item -Recurse -Force $env:NUPKG_PATH -ErrorAction SilentlyContinue

    - name: Download nupkg artifact
      id: dl-commonhelpers-nupkg
      uses: actions/download-artifact@v8
      with:
        name: ${{env.PKG_NAME}}

    - name: Confirm package exists
      run: |
        if (-not (Test-Path -LiteralPath ${{steps.dl-commonhelpers-nupkg.outputs.artifact-path}} -PathType Leaf)) {
          throw "NuGet package file not found at ${{steps.dl-commonhelpers-nupkg.outputs.artifact-path}}"
        }
        Write-Host "NuGet package found at ${{steps.dl-commonhelpers-nupkg.outputs.artifact-path}}"

    - name: Sign NuGet package
      run: |
        $sacTools = "C:\Program Files\SafeNet\Authentication\SAC\x64\SACTools.exe"
        & $sacTools /Token /LogOn /Password "${{secrets.DIGICERT_HARDWARE_TOKEN_PASSWORD}}"
        if ($LASTEXITCODE -ne 0) { throw "SACTools token logon failed with exit code $LASTEXITCODE" }

        $thumbprint = "8BC202959D2DCE3EEB3A489D8348B5C9B6B64A97"
        $certificate = Get-Item -LiteralPath "Cert:\CurrentUser\My\$thumbprint" -ErrorAction Stop
        $certificateFingerprint = $certificate.GetCertHashString([Security.Cryptography.HashAlgorithmName]::SHA256)

        dotnet nuget sign ${{steps.dl-commonhelpers-nupkg.outputs.artifact-path}} `
          --certificate-fingerprint $certificateFingerprint `
          --certificate-store-location CurrentUser `
          --certificate-store-name My `
          --hash-algorithm SHA256 `
          --timestamper http://timestamp.digicert.com `
          --timestamp-hash-algorithm SHA256 `
          --verbosity detailed
        
        if ($LASTEXITCODE -ne 0) { throw "dotnet nuget sign failed with exit code $LASTEXITCODE" }

    - name: Upload signed NuGet package
      uses: actions/upload-artifact@v7
      with:
        name: ${{env.PKG_NAME}}
        path: ${{steps.dl-commonhelpers-nupkg.outputs.artifact-path}}
        if-no-files-found: error
        retention-days: 30
        overwrite: true
